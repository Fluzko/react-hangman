import { render } from "@testing-library/react";
import Hanger from ".";

describe("Hanger", () => {
  it("should render", () => {
    render(
      <Hanger>
        <></>
      </Hanger>,
    );
  });
});
